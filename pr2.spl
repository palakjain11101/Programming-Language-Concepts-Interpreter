Bool bool = True
Matrix x = getLists
List one = get (0) x
List two = get (1) x
List three = get (2) x
List newList = []
while (bool) {
   if (empty three){
      Bool bool = False
   } else {
      Int c = pop (three)
      push c newList
	    Int b = pop (two)
	    push b newList
	    Int a = pop (one)
	    push a newList
      Int d = a+b
      Int e = b+c
      push d newList
	    push e newList
  }
}
return(newList)