Bool bool = True
Matrix x = getLists
List one = get (0) x
List newList = []
Int sum = 0
while (bool) {
   if (empty one) {
      Bool bool = False
   } else {
      Int a = pop (one)
      Int sum = sum + a
	    push sum newList
   }
}
return(newList)